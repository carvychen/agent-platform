# Test + local-dev dependencies — loaded by CI and local developers only.
-r requirements.txt

pytest==9.0.3
pytest-asyncio==1.3.0
respx==0.22.0
asgi-lifespan==2.1.0

# Local-run entry point (`scripts/run_local.py`). `uvicorn` is already a
# transitive dep of `mcp`, but pin it here explicitly since the local-run
# script imports it directly.
uvicorn==0.46.0
